kernel=Image

# RockOS rpi5 firmware config
disable_overscan=1

# KMS graphics for cage (Pi 5 variant)
dtoverlay=vc4-kms-v3d-pi5

# Waveshare 7" HDMI panel (1024x600, USB-HID touch WS170120) — driven
# directly over HDMI, no DSI overlay needed. The DSI ribbon variant of
# this panel is parked (never brought up; needs 22-pin cable).
# Enabling the DSI overlay with no ribbon attached creates a phantom
# DSI-1 output that cage (EXTEND mode) puts the app on — leaving HDMI
# black. Keep this disabled while the panel is HDMI-connected.

dtparam=audio=off
